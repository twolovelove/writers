- **2026-07-06**: 최초 작성. `entries` Supabase 백업 구현 직후 시점 기준으로 항목 정리.
- **2026-07-06**: P0 ① 저작권 귀속 이용약관 추가 완료(`TermsOfService.tsx`). 관리자 판별이 이미 `admins` 테이블 단일 기준으로 정리되어 있음을 확인해 "ADMIN_EMAIL 중복" 항목 정정.
- **2026-07-06**: P0 ② 완료 — `Settings.tsx` 탈퇴 플로우에 모음집 PDF 저장 유도 버튼 추가. P0 ③ 완료 — `schema.sql`이 라이브 프로젝트에 이미 적용되어 있음을 확인, RLS 체크리스트 문서화. **P0 전 항목 완료.**
- **2026-07-06**: P1 ④ 완료 — GitHub Actions(`.github/workflows/ci.yml`) 추가. push/PR마다 `npm run lint`, `npm run build`(타입 체크 포함) 자동 실행, 아직 배포 자동화는 없음(호스팅 미정).
- **2026-07-06**: P1 ⑤ 완료 — `getStreak()` 추가, 대시보드에 연속 작성일 표시. 관리자 이메일 중복 관련 문구가 남아있던 관리 포인트 표를 함께 정정.
- **2026-07-06**: 기술 부채 "테스트 없음" 해소 — Vitest+jsdom 도입, `feedback.test.ts`/`archive.test.ts` 추가(총 23개 케이스), CI에 `npm run test` 단계 연결.
- **2026-07-06**: P1 ⑥ 코드 완료 — `src/lib/analytics.ts` 추가해 GA4 연동 준비(측정 ID 없으면 무동작), 글쓰기 시작/완료/미완료 이탈 이벤트 계측. 실제로 데이터가 쌓이려면 GA4 속성 생성과 측정 ID 입력이 필요(사용자 액션).
- **2026-07-06**: P1 ⑦ 완료 — 초기 타겟을 "매일 일기 쓰고 싶은 직장인"으로 결정, 온보딩 카피와 기본 카테고리를 반영. **P1 코드 작업 전 항목 완료.**
- **2026-07-06**: P0·P1 라운드 마무리. 남은 P2(공유 이미지 카드/수익 모델/푸시 알림)는 지금 만들면 검증할 유저가 없어 착수하지 않고, [로드맵.md](./로드맵.md)에 실행 메모만 남겨둠 — 유저나 매출 신호가 생기면 이 문서부터 다시 확인할 것.
- **2026-07-06**: 하루 1회 완료 잠금(`useDailyLock`) 관련 논의 정리 — 코드 확인 결과 1,000자를 채워도 자동 잠김이 아니라 유저가 완료 버튼을 직접 눌러야 잠기는 구조임을 확인. 이 제약을 없애기보다 워들/BeReal류처럼 서비스 정체성으로 유지하기로 결정.
- **2026-07-06**: 기술 부채 "에러 모니터링 없음" 해소 — `@sentry/react` 도입, `src/lib/monitoring.ts` + 전역 `ErrorBoundary`(`src/components/ErrorFallback.tsx`) 추가, `pushEntry`/`useEntrySync`의 조용히 흡수되던 실패를 `captureError`로 연결. 이걸로 원래 PM 점검 항목 전 항목 완료.
- **2026-07-06**: Sentry DSN 발급·GA4 측정 ID 발급 둘 다 **지금은 미루기로 결정**. 아직 배포 전(로컬만 사용)이라 "프로덕션 에러/트래픽 모니터링"이 당장 필요한 상황이 아님 — 코드는 이미 대기 상태(env var 없으면 무동작)이니 실제 배포하는 시점에 두 개 다 같이 발급받아 넣으면 됨.
- **2026-07-06**: 위 미룸 결정을 실행 가능한 형태로 블록화 — [로드맵.md](./로드맵.md)에 "배포 시점 직전(P0.5)" 행 추가(Sentry DSN, GA4 측정 ID, 호스팅처 확정 후 CD 연결 3가지 체크리스트).
- **2026-07-06**: 탈퇴(`handleWithdraw`) 하드 딜리트 검증 — `entries` 테이블 행 삭제 + 로그아웃만 하고 `auth.users` 계정 자체는 지워지지 않는 gap을 확인함. 완전 삭제하려면 서비스 롤 키가 필요한 `supabase.auth.admin.deleteUser()`를 Edge Function으로 감싸야 함. 아직 실사용자가 없어 지금 당장 리스크가 실현되지는 않는다고 판단해 구현은 보류하고 [로드맵.md](./로드맵.md) 법률/보안/개인정보 표와 P0.5 체크리스트에 기록만 남김 — 실사용자 생기기 전, 배포 직전에 처리.
- **2026-07-06**: 첨삭 기능 on/off 관리자 메모 구체화 — 유저별 토글(UX, `Settings.tsx`)과 관리자 전역 킬스위치(비용 통제)를 분리해서 봄. 현재 첨삭이 규칙 기반이라 API 토큰 비용이 없으므로 전역 킬스위치는 첨삭을 생성형 AI로 고도화하는 시점에나 필요해짐 — 지금은 둘 다 구현 보류, 방향만 문서화.
- **2026-07-06**: 위 gap을 바로 구현하기로 결정("나중에 까먹을 것 같다") — `supabase/functions/delete-account` Edge Function 추가(요청자 JWT로 본인 확인 → 서비스 롤 키로 `auth.admin.deleteUser()` 호출). `schema.sql`에 `entries`/`reviews`/`admins`의 `user_id` FK를 `on delete cascade`로 변경해 계정 삭제 시 관련 행도 함께 정리되게 함. `Settings.tsx`의 `handleWithdraw`가 이 함수를 호출하고 실패 시에만 `deleteAllEntries`로 폴백, 안내 문구에서 "관리자에게 문의" 문구 제거. 로컬에 Supabase CLI가 없어(`npx supabase`로 확인, 로그인 필요) 실제 배포는 사용자 액션으로 남김 — 정확한 명령어를 P0.5에 정리(`schema.sql` 재실행 + `supabase functions deploy delete-account`). 빌드·린트 통과 확인.
- **2026-07-06**: 전체 코드 리뷰 진행 — 🔴 크리티컬 버그 발견: `entries`가 LocalStorage에도 캐시되는데 계정 구분이 없고 로그아웃도 이를 지우지 않아, 같은 기기에서 계정을 바꾸면 이전 계정의 글이 새 계정에 노출·반영될 수 있었음(개인정보 안내 문구와 실제 동작 불일치). 🟡 부가 발견: 동기화 충돌 해결(`updatedAt` 비교)이 클라이언트 기기 시계를 신뢰해, 기기 시계가 어긋나면 최신 글이 조용히 덮어써질 수 있었음.
- **2026-07-06**: 위 두 건 모두 수정 — LocalStorage 캐시/병합 로직(`useEntrySync`, `getAllDrafts`, `applyNewerRemoteEntry`) 전면 제거, `entries`가 Supabase만을 유일한 저장소로 쓰도록 재구조화(`useEntries`가 로그인 시 1회 로드해 대시보드/아카이브/모음집/에디터가 공유하는 캐시로만 사용, 저장 시 서버가 돌려준 최신 값으로 캐시 갱신). `schema.sql`에 `entries_set_updated_at` 트리거 추가해 `updated_at`을 서버 시각(`now()`)으로만 채우게 함. `Settings.tsx`의 "이 기기의 글 데이터 삭제" 섹션은 더 이상 지울 로컬 데이터가 없어 제거, 탈퇴 플로우도 로컬 정리 없이 서버 삭제+로그아웃만 하도록 단순화. `archive.ts`/`archive.test.ts`도 LocalStorage 의존 함수 제거에 맞춰 정리(테스트 17개 모두 통과, 빌드·린트 통과). **주의**: 새 트리거는 아직 라이브 Supabase에 미반영 — 배포 전 SQL 편집기 재실행 필요(P0.5 참고). 또한 로컬 캐시 제거로 "오프라인에서도 일단 로컬엔 저장됨" 안전망이 없어졌음 — 저장 시점에 네트워크가 끊기면 그 저장은 유실될 수 있음(재시도 큐 없음, 의도적 트레이드오프). Google OAuth 로그인이 필요해 이번 변경은 자동화 테스트로 실제 로그인 플로우까지는 검증 못 함 — 배포 전 수동 스모크테스트 필요(P0.5에 추가).
- **2026-07-06**: "완료 후 수정 불가" 논의 — 완료된 글의 수정 기능 자체는 추가하지 않기로 확정(하루 1회 완료 잠금과 같은 맥락의 서비스 정체성). 대신 완료 직전에 되돌릴 수 없다는 걸 부드러운 문구로 미리 알려주기로 하고, `Editor.tsx`의 완료 버튼 위에 "완료하면 오늘 글은 그대로 간직돼요. 다시 고쳐 쓸 수는 없으니, 한 번 더 읽어보고 눌러주세요."를 추가(목표 글자 수를 채웠지만 아직 완료 전일 때만 노출). 블로킹 confirm 창 대신 조용한 안내 문구로 처리해 리추얼 톤 유지.
- **2026-07-07**: 관리자 메모 2~4번 구현 — `ReviewWidget.tsx` 개선. (3) 전송 성공(`status === 'sent'`) 후 5초 뒤 자동으로 모달이 닫히도록 `setTimeout` 추가(언마운트/재열림 시 정리). (4) 작성 중인 메시지를 `localStorage`(`writer:reviewDraft`)에 실시간 저장해 실수로 창을 닫아도 다음에 열 때 이어 쓸 수 있게 함, 전송 성공 시에만 초안 삭제. (2) 최초엔 팝업 안에 작성자 본인 이메일을 노출하는 방식으로 구현했으나, 실제 의도는 "관리자가 리뷰 목록에서 유저 ID를 볼 수 있게"였음을 확인해 팝업 쪽 문구는 제거하고 `AdminReviews.tsx` 리뷰 카드에 `user_id`를 표시하도록 수정(`user_id`는 이미 `reviews` 테이블에 저장되어 있었고 RLS로 관리자만 조회 가능한 값이라 새로 수집하는 정보는 아님). 빌드·린트 통과 확인.
- **2026-07-07**: 완료 문구 위치·연출 조정 — "잠시 후 창이 닫혀요" 안내를 감사 문구 아래 줄로 내리고, 5→4→3→2→1 실시간 카운트다운으로 표시(`{n}초 후 창이 닫혀요`).
- **2026-07-07**: 관리자가 유저 ID를 볼 수 있게 한 것이 개인정보처리방침에 어긋나는지 점검 — `reviews.user_id`는 이전부터 이미 수집·저장되고 있었고(신규 수집 아님), RLS상 이미 관리자만 조회 가능한 항목이라 이번 변경은 "저장되어 있던 값을 관리자 화면에 노출"한 것뿐이라 새로운 개인정보 수집/제3자 제공에 해당하지 않음. 다만 기존 `PrivacyPolicy.tsx` 1번 항목이 "이메일과 메시지만 저장된다"고만 써 있어 실제 저장 항목(계정 식별자/user ID)과 불일치했음 — 이 문구에 "계정 식별자(사용자 ID)"와 "운영자가 문의 대응을 위해 열람" 문구를 추가해 실제 동작과 일치시킴.
- **2026-07-07**: 중간 저장이 실제로 동작하는지 눈에 안 보인다는 피드백 반영 — `ReviewWidget.tsx`에 "중간 저장" 버튼을 입력창 아래 추가, 누르면 그 시점의 입력 내용을 `localStorage`에 저장하고 아주 작은 글씨로 "중간 저장이 되고 있습니다" 문구가 1.5초간 떴다 사라짐. (최초엔 타이핑마다 자동 저장 + on/off 토글로 구현했었으나, 실제 의도는 자동저장이 아니라 유저가 버튼을 눌렀을 때만 그 상태의 글이 저장되는 수동 방식이라는 걸 확인해 토글 체크박스를 걷어내고 버튼 클릭 시에만 저장하도록 수정) 창을 열면 이전에 중간 저장해둔 내용을 불러오고, 전송 성공 시에만 저장된 초안을 삭제. 빌드·린트 통과 확인.
- **2026-07-07**: 첨삭 노트(`FeedbackPanel.tsx`)가 칭찬/제안을 구분 없이 한 줄로 쭉 나열해 필터링 없이 보인다는 피드백 반영 — `generateFeedback()`이 돌려주는 항목을 "잘한 점"/"다듬으면 좋을 점" 두 섹션으로 묶어서 보여주도록 수정. 마지막 총평 항목은 항상 종합 문장(`feedback.test.ts`의 "마지막 총평" 케이스로 고정된 계약)이라 두 섹션에서 제외하고 구분선과 함께 하단에 따로 표시. 로직(`feedback.ts`)은 건드리지 않아 기존 테스트 10개 그대로 통과, 타입체크 통과. Google OAuth 로그인 없이는 실제 작성 플로우로 스모크테스트가 안 되어 `FeedbackPanel`만 별도 프리뷰 엔트리로 렌더링해 스크린샷으로 레이아웃 확인(프리뷰 파일은 확인 후 삭제).
- **2026-07-07**: 글감만 열어보고 나가도 0자 글이 "내가 쓴 글"에 뜨던 버그 수정 — 상세는 아래 개발 메모 5번 참고.
- **2026-07-07**: 리뷰 위젯의 "중간 저장" 기능(localStorage 초안·버튼)을 되돌림 — 상세는 아래 개발 메모 4번 참고.
- **2026-07-07**: 리뷰 위젯 모달의 안내 문구("어떤 기능을 원하세요? 또는 잘 쓰고 계신가요?")가 의견 제출 후 감사 인사 옆에도 그대로 남아있던 것을 수정 — `status === 'sent'`일 때는 안내 문구를 숨기고, 닫기 버튼은 `ml-auto`로 항상 우측 정렬되게 해 레이아웃이 흔들리지 않게 함.
- **2026-07-07**: 오늘 글을 이미 완료해서 직접 확인이 어렵다는 요청으로, 관리자 전용 "언제든 글쓰기" 토글 추가. `useAdminAlwaysWrite`(신규, `localStorage` 기반 — 이 관리자 계정·이 기기에서만 적용)를 `AdminReviews.tsx` 상단에 스위치 UI로 노출, `Dashboard.tsx`는 `isAdmin && alwaysWrite`일 때 `useDailyLock`의 하루 1회 잠금 카드 대신 평소처럼 카테고리 선택·글감 카드를 보여주도록 우회 처리. 서버(Supabase)에는 아무 것도 남기지 않는 순수 로컬 스위치라 스키마 변경·배포 없이 바로 사용 가능. 타입체크·테스트 통과, `AdminReviews`를 별도 프리뷰로 렌더링해 토글 on/off 스크린샷으로 확인(프리뷰 파일은 확인 후 삭제). **주의**: 이 스위치는 잠금만 우회할 뿐 하루 1회 잠금 자체(로드맵.md P0 정책 참고)를 없애는 건 아님 — 일반 유저 경험은 그대로.
- **2026-07-07**: 문서-코드 불일치 정리 — `src/types/index.ts`의 `DraftEntry` 주석이 "LocalStorage에 저장되는"이라는 옛 설명(로컬 캐시 제거 이전 문구)을 그대로 두고 있어 "Supabase entries 테이블의 행 데이터 구조"로 수정. `README.md`의 "Supabase 설정" 섹션도 "배포 전까지는 탈퇴 시 글 데이터만 삭제되고 계정은 남는다"는 표현 대신 "Edge Function 배포 후에는 계정과 모든 데이터가 완전히 삭제된다"로 명확화(Edge Function 자체는 아직 미배포, P0.5 참고).
- **2026-07-07**: 리뷰 위젯 의견 글자수 100자 제한 추가(`input` → `textarea` + `maxLength`, 실시간 카운터 표시). 라이브 Supabase의 `admins`/`reviews`/`entries` RLS 정책을 직접 조회해 이상 없음을 확인했으나, 예방 차원에서 `schema.sql`에 기존 정책을 이름과 무관하게 지우고 재생성하는 방어 블록 추가(상세는 아래 개발 메모 8번).
- **2026-07-07**: 위 커밋을 사용자가 라이브 Supabase SQL 편집기에서 `schema.sql` 전체 재실행 완료. 이번 RLS 정책 방어 블록뿐 아니라, 2026-07-06부터 미반영 상태로 남아있던 `entries_set_updated_at` 트리거(서버 시각으로 `updated_at` 채우기)와 탈퇴 시 `on delete cascade` FK 3종도 이 재실행으로 함께 라이브 반영됨 — [로드맵.md](./로드맵.md) P0.5 체크리스트의 관련 항목 해소.
- **2026-07-07**: OPERATIONS.md가 너무 커져서 `operations/` 폴더로 분리 — 개발/운영/디자인/로드맵/README 5개 파일로 재구성. 상세는 [operations/INDEX.md](./INDEX.md) 참고. (이 인덱스 파일은 2026-07-08에 루트 `README.md`와 이름이 겹쳐 헷갈린다는 피드백으로 `operations/INDEX.md`로 이름을 바꿈 — 내용은 그대로)
- **2026-07-08**: 자동저장은 되는데 "저장 중입니다" 같은 상태 표시가 전혀 없고 임시 저장 버튼도 없다는 피드백으로 `Editor.tsx`에 "임시 저장" 버튼 추가, `useDraft.ts`가 `saveStatus`(`idle`/`saving`/`saved`/`error`)를 노출해 버튼 옆에 "저장 중.../저장됨/저장 실패" 문구가 뜨게 함(자동저장이 돌 때도 같은 문구가 갱신되므로 자동저장 상태 표시 겸용). `saveNow()`가 기존엔 저장 완료를 기다리지 않고 끝났는데, 상태 갱신을 위해 `persist()` 프로미스를 반환하도록 수정.
  - **버그 발견·수정**: 위 작업 중 실제 화면에서 타이핑을 멈춰도 "저장 중/저장됨"이 끝없이 반복되는 걸 발견. 원인은 `useDraft.ts`의 `useDebounce({ title, content }, 1000)`에 매 렌더마다 새로 생성되는 객체 리터럴을 넘기고 있었던 기존 버그 — `useDebounce` 내부 `useEffect`가 객체 참조로 변경 여부를 판단해서, 값이 그대로여도 렌더될 때마다 디바운스 타이머가 계속 리셋·재실행됐음. 원래도 이 때문에 에디터를 열어두기만 해도 배경에서 불필요한 Supabase upsert가 주기적으로 반복됐을 가능성이 있으나, 이번에 `saveStatus`로 화면에 노출되기 전까지는 조용히 묻혀 있었음(상태 갱신 자체가 재렌더를 유발해 루프를 더 눈에 띄게 만듦). `title`/`content`를 객체로 묶지 않고 각각 개별 문자열로 `useDebounce`에 넘기도록 수정해 해결(문자열은 값 비교라 실제로 안 바뀌면 타이머가 리셋되지 않음). 수정 후 8초 대기 시 저장 호출이 1회만 발생하는 것을 프리뷰 하네스(로그인 없이 `Editor` 단독 렌더링, Supabase 호출은 지연 있는 가짜 응답으로 대체)로 확인, 프리뷰 파일은 확인 후 삭제. 빌드·린트 통과.
- **2026-07-08**: 배포 후 월 100명 유저 확보를 목표로 마케팅 플랜 논의 — 타겟 유저("매일 일기 쓰고 싶은 직장인")가 이미 확정돼 있어 새 포지셔닝 없이 온보딩 카피 그대로 활용 가능하다는 점, 예산 0원 기준이라 유료 광고 대신 메이커/사이드프로젝트 커뮤니티와 글쓰기·리추얼 커뮤니티 침투가 우선이라는 점을 정리. 실행 항목을 개발/운영/마케팅 3개 TODO로 나눠 이 문서와 [운영.md](./운영.md)에 반영(코드 변경 없음).
- **2026-07-08**: 대시보드/에디터 구조 개편 — 계기는 "하루 1회 완료 잠금 때문에, 완료 못 한 카테고리의 초고는 영영 이어 쓸 수 없는 채 '내가 쓴 글'에 박제된다"는 논의(먼저 Archive/Compilation을 `completed`만 보이게 필터링). 근본적으로 여러 카테고리에 걸쳐 미완성 초고가 생기는 것 자체를 막기 위해, 유저가 카테고리를 고르던 방식을 없애고 `data/prompts.ts`의 `getTodaysWriting(date)`가 카테고리+글감을 하루 하나로 결정론적으로 정하도록 변경(기존 `getPromptForDate`는 그대로 두고 감싸는 형태). `Dashboard.tsx`는 카테고리 선택 UI(`CategorySelector`)와 글감 미리보기(`PromptCard`)를 모두 없애고 "글쓰기 시작" 버튼 하나만 남김 — 글감 내용(제목·설명)은 `Editor.tsx` 진입 후에만 공개, `EntryView.tsx`의 라벨도 "오늘의 추천 글감"으로 통일. 재추첨(다른 글감으로 바꾸기) 기능은 의도적으로 넣지 않음 — 워들처럼 "그날은 그 글감만"이 정체성에 더 맞다고 판단(사용자 결정). 이제 카테고리가 애초에 하루 하나만 노출되므로 미완성 초고 다중 생성 자체가 구조적으로 불가능해짐 — 아래 개발 메모 5번의 "여러 카테고리 써보는 게 오히려 장점" 결정(2026-07-07)은 이번 개편으로 뒤집힘. `CategorySelector.tsx`/`PromptCard.tsx` 컴포넌트 삭제. 빌드·린트·테스트(17개) 통과.
- **2026-07-08**: 대시보드 카피 중복 정리(UI/UX 리뷰) — "오늘은 어떤 글을 써볼까요?"가 헤더 설명문과 글쓰기 카드에 똑같이 두 번 나오던 것을 역할 분리로 해소(헤더는 습관 설명만, 카드가 실제 질문+CTA 담당). 이어서 (1) 제목 "오늘의 글쓰기"와 버튼 "오늘의 글쓰기 시작하기"가 시각적으로 가까운 거리에서 문구가 겹치던 것을 버튼을 "시작하기"로 축약해 해소, (2) 스트릭 뱃지("N일째 연속 작성 중")와 습관 설명문이 같은 메시지("매일 쓰는 습관")를 중복 전달하던 것을 스트릭이 있을 때(`streak > 0`)는 설명문을 숨기는 조건부 렌더링으로 해소. 로그인 세션 없이는 실제 화면 확인이 어려워, `Dashboard`만 mock session/entries로 격리 렌더링하는 프리뷰 하네스로 스트릭 유무 두 상태를 스크린샷 확인(프리뷰 파일은 확인 후 삭제). 타입체크 통과.

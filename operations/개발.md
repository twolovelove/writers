# 개발 (Dev)

> [operations/INDEX.md](./INDEX.md)에서 전체 문서 구조를 먼저 확인하세요.

---

## 할 일 (TODO)

### 배포 전 필수 (P0.5 — [로드맵.md](./로드맵.md) 참고, 마케팅 착수의 전제조건)
- [ ] `VITE_SENTRY_DSN` 발급 및 주입
- [ ] `VITE_GA_MEASUREMENT_ID` 속성 생성 및 주입
- [x] ~~호스팅처(Vercel/Netlify 등) 확정 후 GitHub Actions에 CD 연결~~: Vercel로 배포 완료(2026-07-09), 배포 도메인 `https://writers-iota.vercel.app`. Vercel 환경변수에 `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` 주입, Supabase Auth 리다이렉트 URL에 배포 도메인 등록, Google Cloud Console OAuth 클라이언트 "승인된 자바스크립트 원본"에도 배포 도메인 등록 완료. 구글 로그인 실제 플로우도 지인이 일반 유저로 테스트 완료(탈퇴까지 정상 동작 확인)
- [x] ~~`delete-account` Edge Function 배포~~: 2026-07-09 배포 완료 (`npx supabase functions deploy delete-account --project-ref gsyjgdkllgofxiqkvmzz --use-api`, status `ACTIVE`, `verify_jwt: true`)
- [x] ~~Google 로그인 실제 플로우(글쓰기→저장→대시보드/아카이브) 수동 스모크테스트~~: 2026-07-09 지인이 일반 유저로 테스트 완료

### 배포 이후, 런칭 전 검토 (`operations/memo.md` 메모 반영)
- [ ] 모바일 반응형 점검 — 커뮤니티/SNS발 유입은 모바일 비중이 클 가능성이 높아, 운영.md 마케팅 TODO의 Phase 1 전에 확인
- [x] ~~완료 화면 공유 카드(인스타용 export) 제작 여부 결정~~: 사용자 요청으로 착수 결정, 2026-07-08 구현 완료(`EntryView.tsx`에 개별 글 단위 이미지 카드 export 추가 — 상세는 log.md 참고). 마케팅 TODO Phase 1의 SNS 채널 전환율에 활용 가능해짐

---

### 기능 영역별 개발 관리 포인트

| 기능 영역 | 개발 관리 포인트 |
| --- | --- |
| **글쓰기 에디터**<br>(`Editor.tsx`, `EditorToolbar`) | 자동저장 디바운스(`useDraft.ts`가 `title`/`content`를 개별 문자열로 `useDebounce`에 넘김 — 객체로 묶어서 넘기면 매 렌더마다 새 참조가 생겨 타이머가 계속 리셋되는 버그가 재발함, 2026-07-08 참고), `saveStatus` 기반 저장 중/저장됨 표시, 서식 적용 로직(`textFormat.ts`) 정상 동작 유지 |
| **대시보드 / 오늘의 글감 추천**<br>(`Dashboard.tsx`, `data/prompts.ts`) | 카테고리·글감 모두 `getTodaysWriting(date)`가 결정론적으로 하루 하나만 정함(유저 선택 UI 없음, 2026-07-08 리디자인) — 홈에는 "글쓰기 시작" 버튼만 있고 실제 글감 내용(제목·설명)은 `Editor.tsx` 진입 후에만 보여줌. `CategorySelector`/`PromptCard` 컴포넌트는 이 개편으로 제거됨(재도입 시 주의) |
| **첨삭 노트**<br>(`feedback.ts`, `FeedbackPanel`) | 규칙 기반 로직 — `feedback.test.ts`에 회귀 테스트 있음, 새 규칙 추가 시 테스트도 같이 갱신 |
| **아카이브 / 모음집**<br>(`ArchiveCalendar`, `Compilation`) | 인쇄/PDF 저장 로직 유지, 월별 데이터 집계 정확성 |
| **글 내보내기**<br>(`EntryView.tsx`, `utils/exportEntry.ts`) | 개별 글 단위 TXT/Markdown/이미지 카드(PNG) export — 카드는 Canvas 2D로 직접 그림(외부 라이브러리 없음), 폭 초과 시 폰트 축소·말줄임표 처리(`fitBody`). `buildTextExport`는 순수 함수라 테스트 있음(`exportEntry.test.ts`), 캔버스 렌더링 로직은 jsdom에서 검증 불가 |
| **로그인 & 글 데이터**<br>(`useSession`, `useEntries`, `useDraft`, `lib/entries.ts`) | `entries`는 이제 Supabase가 유일한 저장소(LocalStorage 캐시 없음) — RLS 정책 유지, 새 테이블 추가 시 아래 "RLS 체크리스트" 준수 |
| **리뷰 위젯 & 관리자 페이지**<br>(`ReviewWidget`, `AdminReviews`) | 관리자 판별은 `admins` 테이블 단일 기준(`useIsAdmin.ts`) — 새 관리자 추가 시 이메일 하드코딩 대신 이 테이블에 행 추가 |
| **설정 / 개인정보 / 탈퇴**<br>(`Settings.tsx`, `PrivacyPolicy.tsx`) | 탈퇴는 `delete-account` Edge Function으로 `auth.users` 계정 자체를 지움(`entries`/`reviews`/`admins`는 FK cascade로 함께 정리) — 실패 시에만 `deleteAllEntries`로 폴백. Edge Function 미배포 상태면 폴백 경로만 도는 셈이니 배포 여부([로드맵.md](./로드맵.md) P0.5) 확인 |

### 개발이 별도로 계속 관리해야 할 기술 부채
- ~~`supabase/schema.sql` 미적용~~: 확인 결과 이미 라이브 Supabase 프로젝트에 적용되어 있음(2026-07-06 확인). `entries`/`admins`/`reviews` 테이블과 RLS 모두 동작 중.
- ~~`ADMIN_EMAIL` 중복~~: 확인 결과 이미 해소됨 — 관리자 판별은 `admins` 테이블(`schema.sql`) 단일 기준이고 `config.ts`의 `SUPPORT_EMAIL`은 문의처 안내용으로만 쓰임 (`src/hooks/useIsAdmin.ts` 참고).
- ~~CI/CD 없음~~: `.github/workflows/ci.yml` 추가(2026-07-06). `main` push·PR마다 lint+타입체크+빌드 자동 실행. 아직 배포(호스팅) 자체는 연결 안 함 — 배포처가 정해지면 CD 단계 추가 필요.
- ~~테스트 없음~~: Vitest 도입(2026-07-06). `feedback.test.ts`(첨삭 규칙 10개 케이스), `archive.test.ts`(정렬·병합·스트릭 등 유닛 테스트) 추가, CI에 `npm run test` 단계 연결. 새 규칙/로직 추가 시 테스트도 같이 갱신할 것.
- ~~에러 모니터링 없음~~: `@sentry/react` 도입(2026-07-06). `src/lib/monitoring.ts`가 `VITE_SENTRY_DSN` 설정 시에만 초기화, `main.tsx`에서 앱 전체를 `ErrorBoundary`(`src/components/ErrorFallback.tsx`)로 감싸 렌더링 중 에러를 잡고, 조용히 흡수되던 `pushEntry` 실패도 `captureError`로 전송하도록 연결. DSN 미설정 시 콘솔 경고만 남기고 앱 동작에는 영향 없음. **Sentry 프로젝트 생성·DSN 발급은 지금 미룸** — 아직 배포 전(로컬만 사용)이라 "프로덕션에서 유저가 겪는 에러를 모른다"는 리스크 자체가 실현될 상황이 아님. 배포 시점에 GA4 측정 ID와 함께 챙기면 됨(2026-07-06 결정).
- ~~LocalStorage에 글이 캐시돼 계정 간 데이터가 섞일 수 있음~~: 전체 코드 리뷰(2026-07-06)에서 발견 후 즉시 수정. 상세는 아래 업데이트 로그 2026-07-06 "계정 간 글 데이터 혼입" 항목 참고.

### RLS 체크리스트 — Supabase에 새 테이블을 추가할 때마다
1. `alter table <table> enable row level security;`를 빠뜨리지 않았는가?
2. 정책이 "본인 데이터만" 접근 가능하게 `auth.uid() = user_id` 조건을 걸고 있는가? (전체 공개가 목적이 아니라면)
3. 관리자 전용 열람이 필요하면 이메일 하드코딩이 아니라 `admins` 테이블을 참조하는가? (`reviews` 테이블 정책 참고)
4. 로컬에서 다른 계정 두 개로 로그인해 "내 데이터만 보이고 남의 데이터는 안 보이는지" 직접 확인했는가? (RLS는 서버 쪽 격리만 보장 — 클라이언트가 이 데이터를 LocalStorage 등 계정과 무관한 곳에 캐시해두면 이 격리가 클라이언트 쪽에서 깨질 수 있다는 걸 별도로 체크할 것. 2026-07-06에 `entries`가 이 문제로 실제 크리티컬 버그였음 — 아래 업데이트 로그 참고)
5. `supabase/schema.sql`에 반영한 뒤, README의 "Supabase 설정" 안내대로 실제 프로젝트 SQL 편집기에도 실행했는가?
6. 새 테이블에 `updated_at` 같은 "최신 여부 판단" 컬럼이 있다면, 클라이언트 기기 시계가 아니라 서버(`now()`, 트리거)가 채우게 했는가? (기기 시계가 어긋나면 최신 데이터가 오래된 데이터에 덮어써질 수 있음)